% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{influx_get_update}
\alias{influx_get_update}
\title{Incrementally update InfluxDB data}
\usage{
influx_get_update(
  measurements,
  config = influx_config(),
  from = c("files", "data"),
  data = NULL,
  end = NULL,
  bucket = "dp23",
  tags = NULL,
  tz = "Australia/Sydney",
  default_start = "2023-12-01",
  chunk_by = "month",
  save_files = TRUE,
  output_dir = "data/raw/influx",
  verbose = TRUE
)
}
\arguments{
\item{measurements}{Character vector of measurement names.}

\item{config}{Connection config from \code{\link[=influx_config]{influx_config()}}.}

\item{from}{Where to find existing data: \code{"files"} (default) or \code{"data"}.}

\item{data}{An existing tibble when \code{from = "data"}.}

\item{end}{End time for the update (default: now).}

\item{bucket}{InfluxDB bucket name.}

\item{tags}{Optional named list of tag filters. Names are tag keys, values
are character vectors of allowed values. Multiple values for a single tag
are OR'd; separate tags are AND'd via separate filter steps.
E.g. \code{list(source = "house_1", room = c("bedroom", "kitchen"))}.}

\item{tz}{Timezone.}

\item{default_start}{Fallback start date when no existing data is found.}

\item{chunk_by}{Chunking interval.}

\item{save_files}{Whether to save chunk files.}

\item{output_dir}{Directory for saved files.}

\item{verbose}{Print progress messages.}
}
\value{
A tibble of newly downloaded data.
}
\description{
For each measurement, finds the last available data point and downloads
everything from there to \code{end}. Creates output directories as needed.
}
